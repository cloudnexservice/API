# Production Environment Configuration
# This file is used when running `npm build`
# Change this to your actual HTTPS domain (e.g., https://api.yourdomain.com)
# This ensures all API calls are HTTPS, preventing mixed-content errors

REACT_APP_API_URL=https://your-api-domain.com
REACT_APP_ENVIRONMENT=production
